import React from 'react';

const Banner = ({title}) => {
    return (
        <>
            <div className="header">
                <h1>{title}</h1>
            </div>
        </>
    );
};

export default Banner;
